# Copy to sftp.env (git-ignored) and fill in. Used by ./deploy.sh
SFTP_HOST=xxxxx.wpstaq.com
SFTP_PORT=22
SFTP_USER=xxxxx
SFTP_PASS=xxxxx
# Optional override — defaults to wordpress/wp-content (WPStaq layout)
# REMOTE_BASE=wordpress/wp-content
